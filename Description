(Change to read me ?)
Quick note: 
  -> Tried using the Linux terminal for Git; however, it failed during authentication. So, I decided to use Git inside VSCODE ! 
  -> Let's see how to configure Git inside VSCODE (which also has a terminal !)
